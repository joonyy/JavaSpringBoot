<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="practice.mybatis_on_my_own.mapper.UserMapper">

<select id="findAll" resultType="practice.mybatis_on_my_own.domain.User">
  select * from users
</select>

<select id="findById" parameterType="Long" resultType="practice.mybatis_on_my_own.domain.User">
  select * from users
</select>

<insert id="insert" parameterType="practice.mybatis_on_my_own.domain.User"
        useGeneratedKeys="true" keyProperty="id">
  insert into users (username,email) values (#{username},#{email})
</insert>

<update id="update" parameterType="practice.mybatis_on_my_own.domain.User">
  update users set username = #{username}, email=#{email} where id=#{id}
</update>

<delete id="delete" parameterType="Long">
  delete from users where id=#{id}
</delete>

</mapper>